scalar Date

type Error {
    code: Int!
    error: String!
}

type SimplePagination {
    total: Int!
    page_size: Int!
    offset: Int!
}

type Player {
    id: Int!
    name: String!
    tag: Int!
}
